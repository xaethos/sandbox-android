platform: linux

image_resource:
  type: docker-image
  source:
    repository: yarbelk/androider

inputs:
- name: sandbox-android

run:
  dir: sandbox-android
  path: ./gradlew
  args:
    - test

params:
  TERM: dumb
